apiVersion: 1

providers:
  - name: "Bookstore Dashboards"
    orgId: 1
    folder: "Bookstore POS"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
